#include "shell.h"

/**
 * main - the entry point
 * main point of the name
 * is not thers
 */
{
	exit(127);
			
return (EXIT_FAILURE);
		}
	info->readfd = fd;
	
	populate_env_list(info);

read_history(info);
	hsh(info, av);
	return (EXIT_SUCCESS);
}
